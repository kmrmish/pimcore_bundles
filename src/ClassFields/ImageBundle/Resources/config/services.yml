services:
    # default configuration for services in *this* file
    _defaults:
        # automatically injects dependencies in your services
        autowire: true
        # automatically registers your services as commands, event subscribers, etc.
        autoconfigure: true
        # this means you cannot fetch services directly from the container via $container->get()
        # if you need to do this, you can override this setting on individual services
        public: false

    # controllers are imported separately to make sure they're public
    # and have a tag that allows actions to type-hint services
    ClassFields\ImageBundle\Controller\:
        resource: '../../Controller'
        public: true
        tags: ['controller.service_arguments']

# add more services, or override services that need manual wiring
#    ClassFields\ImageBundle\ExampleClass:
#        arguments:
#            - "@service_id"
#            - "plain_value"
#            - "%parameter%"
    # Example event listener for objects
    # ClassFields\ImageBundle\EventListener\CustomImageUpload:
    #     tags:
    #         - { name: kernel.event_listener, event: pimcore.dataobject.preUpdate, method: onObjectPreUpdate }


parameters:
    CustomImageUploadClass : 'AppBundle\Model\DefaultProduct'
    MainImageFields:
        main1:
            thumbnail: main1
        main2:
            thumbnail: main2
        main3:
            thumbnail: main3
    ImageGallaryFields:
        gallery1:
            thumbnail: gallery1
        gallery2:
            thumbnail: gallery2